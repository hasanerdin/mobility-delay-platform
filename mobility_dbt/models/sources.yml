version: 2

sources:
  - name: raw
    description: "Raw data sources ingested into the system."
    database: mobility
    schema: raw
    tables:
      - name: trips
        description: "Raw trip data ingested from the source system."

      - name: historical_trips
        description: "Historical trip data used for training and analysis."